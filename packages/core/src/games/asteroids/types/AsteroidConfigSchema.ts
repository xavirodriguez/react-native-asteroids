export const AsteroidConfigSchema = {};
export interface AsteroidConfig {
  SCREEN_WIDTH: number;
  SCREEN_HEIGHT: number;
  SCREEN_CENTER_X: number;
  SCREEN_CENTER_Y: number;
  INITIAL_ASTEROID_COUNT: number;
  KEYS: any;
  TRAIL_MAX_LENGTH: number;
}
