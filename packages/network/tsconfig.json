{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "outDir": "./dist", // OutDir configured to "./dist" to stop contamination of src/
        "rootDir": "./src"
    },
    "include": [
        "src"
    ]
}
