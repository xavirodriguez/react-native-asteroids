import { RenderCommandBuffer } from "../rendering/RenderCommandBuffer";

/**
 * @deprecated Use {@link RenderCommandBuffer} instead.
 * Renamed to clarify its purpose and avoid collision with WorldCommandBuffer.
 */
export class CommandBuffer extends RenderCommandBuffer {}
