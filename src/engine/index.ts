/**
 * Compatibility layer for the legacy engine.
 * Re-exports everything from the new canonical core.
 */

export * from "@tiny-aster/core";

// Note: Internal legacy modules like 'legacy/' or 'services/' may still exist
// temporarily in src/engine/ during the final stages of migration.
